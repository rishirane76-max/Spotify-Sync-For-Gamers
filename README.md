# Spotify-Sync-For-Gamers
This .py script will run in background all u have to do is press control key on your keyboard twice to make it show and again to a
Download Both the files and make terminal point to the folder in which files are stored.
You will need to install some frameworks such as PyQt6 to install of of these run this command : pip install -r requirements.txt
then Use : python3 lyric_hud.py then your set.
